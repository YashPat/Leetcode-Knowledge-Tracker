Habit tracker architecture: add `Habit` + `HabitCompletion` SwiftData models (relationship, one row per habit+calendar day, day stored as start-of-day), `HabitTrackerStore` with `toggleToday(habit)`, and `HabitTrackerRootView` wiring that mirrors the LeetCode tab (own `ModelContainer`, `.environment(store)`). UI can stay a stub.
Done when models + store compile, toggling creates/deletes today's completion, and you can explain in your own words why completions are separate records instead of a set of dates on `Habit`.
